# ─── ESP32 Voice Command Assistant — Python Dependencies ─────────────
# Install:  pip install -r requirements.txt

# NLU / AI
nltk>=3.8
scikit-learn>=1.4
joblib>=1.3
pandas>=2.1

# (transformers + torch are still needed for the summarizer command,
#  but the intent classifier no longer requires them.)
transformers>=4.40
torch

# Fuzzy file search
thefuzz[speedup]>=0.22   # includes python-Levenshtein C extension

# Document reading
PyPDF2>=3.0
python-docx>=1.1

# Desktop automation
pyautogui>=0.9
pyperclip>=1.8

# System monitoring
psutil>=5.9

# Brightness control
screen-brightness-control>=0.18

# Speech-to-text (Whisper — ESP32 integration)
openai-whisper>=20231117
soundfile>=0.12

# Serial (ESP32 integration)
pyserial>=3.5

# Testing
pytest>=8.0
pytest-cov>=4.0
